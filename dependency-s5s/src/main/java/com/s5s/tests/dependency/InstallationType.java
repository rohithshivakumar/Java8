package com.s5s.tests.dependency;

/// <summary>
/// Installation type is an enum to represent the way we installed the component
/// </summary>
public enum InstallationType
{
    Explicit,
    Implicit,
    NotInstalled
}
